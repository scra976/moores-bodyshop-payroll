# Copy this file to .env and paste your GitHub token on the GH_TOKEN line.
# .env is gitignored and is never packed into the Windows installer.
#
# Classic PAT: scope public_repo (or repo if the GitHub repo is private)
# Fine-grained PAT: this repository, Contents = Read and write
GH_TOKEN=
